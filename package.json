{
  "name": "curiosity-trail",
  "version": "1.0.0",
  "scripts": {
    "build": "cd web/client && npm run build && cd ../server && npm install",
    "start": "cd web/server && node index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
